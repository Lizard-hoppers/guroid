# OPERATIONS — Gambling Community Bot

Бот: [@GamblingCommunitybot](https://t.me/GamblingCommunitybot) — анкета доступа в Private Gambling Community.

## Где
- Сервер: VPS `193.111.62.16`
- Каталог: `/opt/gambling_community_bot`
- venv: `/opt/gambling_community_bot/.venv` (Python 3.12)
- Сервис: `gambling-community-bot.service`
- БД: `/opt/gambling_community_bot/gambling_community.sqlite3` (источник правды)
- Персистентность: `/opt/gambling_community_bot/bot_persistence.pkl` (состояние диалогов и
  user_data; создаётся при первом взаимодействии, переживает рестарт). Runtime-файл —
  не в git, исключён из rsync. Удаление сбросит незаконченные анкеты в процессе (готовые —
  уже в SQLite/Sheets, не теряются).

## Управление
```bash
systemctl status gambling-community-bot
systemctl restart gambling-community-bot
journalctl -u gambling-community-bot -n 100 --no-pager
```

## Конфигурация (`.env`)
- `BOT_TOKEN` — токен из @BotFather.
- `ADMIN_IDS` — кому слать новые анкеты, кто может `/export` и `/stats` (через запятую).
- `COMMUNITY_INVITE_URL` — **ЗАГЛУШКА** ссылки «Вступить в сообщество» (блок 7). Заменить на реальный invite (`https://t.me/+...`) и `restart`.
- Google Sheets: пока `GOOGLE_SHEETS_ENABLED=false` — нет ID таблицы.

## Админка / CMS «Контент»
Команды (только для `ADMIN_IDS`):
- `/admin` — раздел «Управление контентом»: **Тексты** (сообщения блоков 1–7), **Кнопки**
  (Основные / Вертикали / Грейды / Инвестор) и **Медиа** (GIF/фото/видео на экранах).
  Правка текста/кнопки — прислать новый текст; медиа — прислать GIF/фото/видео; есть
  «♻ Сбросить»/«🗑 Удалить медиа». Изменения применяются на лету, без рестарта.
  Экран с медиа показывается как фото/гиф/видео, текст экрана становится подписью.
- `/export` — CSV всех анкет. `/stats` — счётчики и статус Sheets.
- `/myid` — узнать свой Telegram ID (доступно всем; для заполнения `ADMIN_IDS`).

CMS-схема: оверрайды в SQLite (`button_settings`, `content_texts`, `media_assets`),
эффективное значение = override → дефолт из `constants.py` (`BUTTON_DEFAULTS`/`TEXT_DEFAULTS`).
Рендер экрана — единый `_show(key)` в flow.py: с медиа шлёт фото/гиф/видео+подпись, без —
текст; переход text↔media пересоздаёт сообщение (Clean Chat, один экран). Профессии (1131) —
данные, в CMS не редактируются. Подписи вертикалей/грейдов привязаны к индексу: правка подписи
не меняет логику и сохраняемые в анкету канонические значения.

## Медиакит (PDF перед инвайтом)
После анкеты, до финального экрана с кнопкой «Вступить», бот шлёт отдельным документом
PDF-медиакит GURO (аудитория, «Гэмблинговый суд», спонсорские тарифы) — показывает ценность
комьюнити в момент максимальной вовлечённости и параллельно работает как тихая воронка
спонсорских лидов.
- Файлы (статический ассет, НЕ в CMS): `assets/media_kit/GURO-Mediakit-RU.pdf`,
  `assets/media_kit/GURO-Media-Kit-EN.pdf`. Язык выбирается по `lang` анкеты.
  Чтобы обновить файл — заменить PDF по тому же пути и `restart` (кэш file_id не используется,
  бот каждый раз читает файл с диска).
- Подпись — CMS-текст `media_kit_caption` / `media_kit_caption_en` (правится как обычный экран
  из `/admin` → Тексты).
- Отправка best-effort (`handlers/flow.py::_send_media_kit`): сбой отправки логируется, но НЕ
  блокирует выдачу инвайт-ссылки. Забаненным (`storage.is_banned`) не отправляется.

## Clean Chat
Бот удаляет сообщения-команды (`/start`, `/admin`, `/myid`, `/export`, `/stats`) и весь
пользовательский ввод (ответы анкеты, тексты/медиа при правке в CMS) — остаётся один
активный экран. На экранах «Другое» (своя вертикаль и своя должность) есть кнопка «Назад»
(`vo_back`/`po_back`) — не тупик.

## Архитектура (Clean Core)
- `config.py` — настройки из окружения.
- `constants.py` — тексты блоков 1–7 + вертикали/грейды/инвестор.
- `professions_data.py` — авто-сгенерирован из ТЗ (вертикаль→грейд→[(code,label)], 1131 поз.).
- `ui.py` — клавиатуры (пагинация профессий).
- `logic.py` — чистые функции (пагинация, карточка, строка таблицы, html_escape).
- `storage.py` — SQLite + мягкие миграции `_ensure_column`.
- `google_sheets_sync.py` — best-effort запись анкет в таблицу.
- `handlers/flow.py` — ConversationHandler всего флоу (Clean Chat: один экран).
- `handlers/admin.py` — `/export`, `/stats`.
- `handlers/group_captcha.py` — капча для новичков в группе (второй уровень).
- `bot.py` — точка входа.

## Второй уровень: капча в группе + меню-ссылки после неё
Бот должен быть **админом группы** с правом **Banning users** (restrict). При входе
новичка `ChatMemberHandler` (CHAT_MEMBER) мутит его (`restrict_chat_member`, все права
False) и шлёт капчу ОДНИМ сообщением: медиа экрана `captcha_welcome` (видео/гиф/фото из
CMS) + приветствие с упоминанием (`tg://user?id=`) ENG/RU со ссылками на главные чаты +
пример «A + B = ?» и кнопки-варианты (`cap:<uid>:<value>`), один верный.
- **Неверный ответ** → тот же приём/фото остаётся, текст+кнопки редактируются на новый
  пример (`edit_message_caption`/`edit_message_text`), пользователь остаётся в муте.
  Капча бесконечна, кик/бан НЕ используются.
- **Верный ответ** → размут (`restrict_chat_member` с дефолтными правами чата), и бот
  **редактирует ТО ЖЕ сообщение** (видео/фото остаётся): текст меняется на
  `captcha_success` (та же инфа про главные чаты, без блока CAPTCHA) + клавиатура
  меняется с вариантов ответа на **5 инлайн-кнопок-ссылок** (`ui.group_menu_kb`) —
  Terms/GuroPSP/GuroAffiliate/iGamingSchool/Collaboration. Сообщение НЕ удаляется —
  остаётся в чате как постоянная памятка со ссылками (как и было по референсу).
  Инлайн-кнопки надёжно открывают ссылку и не пропадают, пока жива сама message —
  никакого якоря/самовосстановления не требуется (в отличие от нижней reply-клавиатуры,
  которую пробовали и отказались — см. ниже).
- Состояние ждущих капчу хранится в `chat_data["captcha"][user_id]` (переживает рестарт).

Конфиг капчи (`.env`): `CAPTCHA_ENABLED` (true/false), `CAPTCHA_OPTIONS` (число кнопок, ≥2),
`CAPTCHA_GROUP_IDS` (белый список ID групп через запятую; пусто = любые группы). Тексты
(`captcha_welcome`, `captcha_success`) и видео — в `/admin` → Тексты/Медиа. Сами 5 кнопок
(текст/ссылка/цвет/премиум-эмодзи/вкл-выкл) — `/admin` → «🔗 Меню группы» (таблица
`menu_buttons`, сидинг дефолтов при первом старте).

**Почему не reply-клавиатура снизу (пробовали и откатили):** Telegram не даёт reply-кнопкам
(под строкой ввода) открывать ссылку по нажатию (проверено: `url` через api_kwargs
рендерится, но не срабатывает), и клавиатура держится только пока жива хотя бы одна её
message — удалили последнюю, и кнопки у пользователей пропадают (тоже проверено на живой
группе). Инлайн-подход (кнопки на самом сообщении капчи) решает оба недостатка сразу.
- Цвет/премиум-эмодзи на кнопках — `style`/`icon_custom_emoji_id` через `api_kwargs`
  (см. `ui.link_button`), приём подсмотрен в Таки Вместе (aiogram) и воспроизведён в PTB.

## Тесты (обязательно перед деплоем)
```bash
cd /opt/gambling_community_bot && .venv/bin/python simulate_bot_tests.py
```
Проверяет данные, логику, хранилище, CMS, капчу (логика + симуляция вход/мут/неверно/верно/
размут, бот-новичок, чужой клик, allowlist, media) и сценарии флоу (включая отправку
медиакита RU/EN и её отсутствие у забаненных). PASS=3258.

## Деплой (по манифесту)
1. backup live-каталога.
2. rsync в `*_candidate`.
3. `simulate_bot_tests.py` в candidate.
4. rsync в live, прогон тестов в live.
5. `systemctl restart` → проверка `journalctl`.
6. удалить candidate.

## ОТКРЫТО / TODO
- [ ] Получить от владельца реальную invite-ссылку → `COMMUNITY_INVITE_URL`.
- [ ] Создать Google-таблицу, выдать доступ `island-summary-bot@ostrovbot.iam.gserviceaccount.com`,
      положить `GOOGLE_SHEETS_SPREADSHEET_ID`, `GOOGLE_SHEETS_ENABLED=true`, при необходимости
      залить накопленные анкеты из SQLite (`/export` → импорт или backfill-скрипт).
- [x] Прислать числовой Telegram ID владельца → `ADMIN_IDS` (1417059280, 8315845804).
- [ ] Капча: заменить заглушки ссылок на главные чаты под реальную группу (CMS, экран
      «Капча в группе»). Видео уже залито. При желании сузить `CAPTCHA_GROUP_IDS` под продакшн-группу.

## GURO ID Mini App (партнёрства/репутация), 09-10.08.2026

Отдельный модуль поверх бота — фиксация подтверждённого сотрудничества между
участниками и репутация (см. ТЗ владельца, п.1-8). НЕ отменяет и не заменяет
план networking-заявки/реф-центра/колеса фортуны (04.08) — это отдельный
раздел общего будущего Mini App.

**Компоненты:**
- `guro_constants.py`, `guro_logic.py` — чистые функции (формула репутации,
  анти-фрод, валидация Telegram WebApp initData).
- `guro_storage.py` — свои таблицы `guro_users`/`partnerships` в ТОМ ЖЕ
  `gambling_community.sqlite3` (WAL включён), `profiles` не трогает.
- `guro_id_api.py` — отдельный aiohttp-процесс/systemd-сервис
  `guro-id-api.service`, слушает `127.0.0.1:8092` (`GURO_ID_API_PORT`),
  раздаёт JSON (`/api/me`, `/api/search`, `/api/partnerships`, `/api/subscribe`)
  и статику собранного фронтенда (`webapp/dist`).
- `handlers/guro_partnerships.py`, `handlers/guro_payments.py` — кнопки
  Подтвердить/Отклонить и Telegram Stars (pre_checkout/successful_payment)
  обрабатывает ТОЛЬКО bot.py (он единственный поллит getUpdates) —
  guro_id_api.py лишь шлёт первое уведомление через разовый `Bot(token=...)`.
- `webapp/` — React+Vite SPA (4 экрана MVP из ТЗ п.5) + `framer-motion`
  (анимированные переходы между табами, staggered-появление карточек/списков,
  тактильная отдача через `Telegram.WebApp.HapticFeedback`, добавлено
  10.08.2026). Собирается ЛОКАЛЬНО (`npm install && npm run build` — на
  сервере Node не установлен) и копируется на сервер как `webapp/dist/`.
  Замена содержимого `dist/` НЕ требует рестарта `guro-id-api.service` —
  aiohttp отдаёт статику с диска на каждый запрос (проверено на практике).
- Menu Button бота (`bot.py` → `post_init`) открывает Mini App напрямую,
  URL — `GURO_ID_WEBAPP_URL` (settings).

**Инфраструктура (важное отличие от предположения в исходном плане — на этом
сервере НЕ nginx, а Webuzo-Apache):**
- Публичный адрес: `https://guro-app.193.111.62.16.sslip.io/` (два новых
  vhost-файла — `/usr/local/apps/apache2/etc/conf.d/guro_id_vhost.conf` (HTTP,
  редирект на HTTPS) и `guro_id_vhost_ssl.conf` (HTTPS, ProxyPass на
  `127.0.0.1:8092`), сертификат Let's Encrypt через `certbot certonly --webroot
  -w /var/www/guro-id-acme` (сам certbot не был установлен — доставлен
  отдельно, `apt install certbot`; автопродление — `certbot.timer`).
- Apache здесь управляется НЕ systemd (`httpd.service` в systemd числится
  неактивным) — бинарник напрямую: конфиг-тест `/usr/local/apps/apache2/bin/httpd
  -t`, применение изменений `/usr/local/apps/apache2/bin/httpd -k graceful`
  (НЕ полный restart — не рвёт соединения других живых сайтов на этом Apache:
  панель Webuzo, Odessa-бот прокси, Island Live Map).
- При переезде на купленный домен: получить новый сертификат
  (`certbot certonly --webroot ...` под новый домен), поменять `ServerName`+
  `SSLCertificateFile/KeyFile` в обоих vhost-файлах, `GURO_ID_WEBAPP_URL` в
  `.env`, `httpd -k graceful`.

**Известное ограничение платформы:** Bot API не отправит сообщение
произвольному `@username` — партнёрство можно подтвердить только с тем, кто
уже проходил анкету бота (есть строка в `profiles`). Это ожидаемо (см. ТЗ:
фиксация уже существующих в комьюнити знакомств), но UI явно сообщает об
ошибке `NO_CONFIRMER_PROFILE`.

**Тюнинг, требующий решения владельца (сейчас — стартовые значения по
умолчанию, не зафиксированы в ТЗ):**
- `guro_constants.CONFIRMATION_WEIGHT = 10.0` — вес одного партнёрства в
  формуле репутации.
- `guro_constants.SUBSCRIPTION_STARS_PRICE = 150` (XTR) и
  `SUBSCRIPTION_DURATION_DAYS = 30` — цена/срок подписки.

**Тесты:** `simulate_bot_tests.py` дополнен блоками `guro_id: initData`,
`guro_id: формула репутации`, `guro_id: storage`, `guro_id: API (aiohttp
routes)`, `guro_id: bot-side partnership/payment handlers` — все через
временные SQLite-файлы и фейковые Bot/Update, реальных Telegram-запросов не
делают.

**Ручная проверка после деплоя (сделать на реальном аккаунте):**
1. В личке с ботом открыть Menu Button (GURO ID) → вкладка «Профиль».
2. Отправить заявку на партнёрство со второго тестового аккаунта → на первом
   должно прийти сообщение с кнопками Подтвердить/Отклонить.
3. Подтвердить → в обоих профилях должна появиться запись.
4. Оплатить тестовую подписку Stars (если тестовый режим недоступен — хотя бы
   убедиться, что `createInvoiceLink` отдаёт рабочую ссылку и `successful_payment`
   правильно проставляет `guro_users.subscription_status`).

## GURO ID — статус-тег в чате (setChatMemberTag), 10.08.2026

По просьбе заказчика: видно прямо в группе, что автор сообщения —
ПОДПИСЧИК GURO ID, без захода в Mini App. Реализовано через нативный
`Bot.set_chat_member_tag` (Bot API 22.7+, метод `setChatMemberTag`) — тег
у ОБЫЧНОГО участника (не только у админов, как `setChatAdministratorCustomTitle`),
поэтому не упирается в лимит ~50 админов на группу. Максимум 16 символов,
эмодзи запрещены Bot API. Подсмотрено у соседнего Island Summary Bot
(`rating.py` — там тем же методом показывается репутация-скор, у нас —
факт активной подписки).

**Потребовало апгрейда `python-telegram-bot` 21.6 → 22.8** (метода не было в
21.x) — прогнан весь `simulate_bot_tests.py` до и после, задеплоено,
регрессий не найдено. Право `can_manage_tags` у бота уже было (видно, что
выдаётся автоматически полным админам) — ручных действий от владельца
группы не потребовалось.

**Один уровень тега, только для подписчиков (переделано 10.08.2026 вечером,
исходно было два уровня GURO ID/GURO ID PRO):** владелец решил, что просто
заход в Mini App / наличие анкеты — дефолтное условие для всех в
сообществе, ничем не выделяет и тега давать не должно. Тег «GURO ID»
теперь появляется ТОЛЬКО при активной платной подписке (150 ⭐/30 дней) —
`guro_tags.target_tag()` возвращает `GC.GURO_TAG` при `storage.is_subscribed()`,
иначе пустую строку. **Снятие тега — тоже через `setChatMemberTag`, но с
`tag=""`** (пустая строка, НЕ `None` — эмпирически проверено на реальном
Bot API 10.08.2026: `tag=None` не гарантированно сериализуется в запрос,
`tag=""` подтверждённо снимает тег, `getChatMember` после этого отдаёт
`tag=None`).

**Компоненты:**
- `guro_tags.py` — ядро: `target_tag()` (GURO_TAG «GURO ID» с активной
  подпиской / `""` без неё), `sync_member_tag()` (пропускает
  админов/овнера/left/banned и НЕ перетирает тег, который участник выставил
  себе сам вручную — сравнивает с `GC.GURO_TAGS`), `sync_member_tag_standalone()`
  (разовый `Bot(token=...)` для процессов без Application), `sync_all_members()`
  (пакетный обход с паузой между вызовами).
- Точки вызова: `guro_id_api.py` `handle_me` (тег освежается при каждом
  открытии профиля в Mini App — например, сразу проявляется, если подписку
  только что купили), `handlers/guro_payments.py`
  `on_guro_successful_payment` (тег ставится сразу после оплаты, не дожидаясь
  следующего открытия Mini App), `guro_tags_sync.py` (systemd timer
  `guro-tags-sync.timer`, раз в час — единственное место, где тег
  СНИМАЕТСЯ при истечении подписки, т.к. для этого события нет триггера в
  реальном времени; заодно подчищает то, что не долетело сразу из-за
  сети/лимитов).
- `guro_storage.list_guro_user_ids()` — только те, кто хоть раз коснулся
  GURO ID (таблица `guro_users`), не вся группа — избегаем сотен тысяч лишних
  вызовов `getChatMember` по всей группе на 5000+ человек.

**Проверено на проде 10.08.2026:** ручной запуск `systemctl start
guro-tags-sync.service` реально проставил тег «GURO ID» двум живым
участникам группы, а после переделки на «только подписчики» — реально
СНЯЛ его с тех же двух (не подписаны) через `tag=""`, подтверждено
повторным `getChatMember` (`tag=None`).

**Тесты:** блок `guro_id: статус-тег в чате (guro_tags)` в
`simulate_bot_tests.py` (нет тега без подписки, появляется тег с
подпиской, снимается при истечении, skip админа, skip ручного тега,
`sync_all_members`) + точечные проверки в существующих блоках `guro_id: API`
и `guro_id: bot-side partnership/payment handlers`. PASS=3356 FAIL=0.

**Заодно 10.08.2026: убран бонус +5 к репутации за заполненную анкету**
(`SCREENING_BONUS`) — владелец решил, что анкета это дефолтное условие
входа в сообщество (гейт капчи для всех), а не достижение, которое должно
поднимать рейтинг. `guro_logic.initial_reputation()` теперь просто
`BASE_REPUTATION (50) + tenure_bonus(...)` — стартовая репутация свежего
профиля 50 (было 55), потолок стартовой репутации без партнёрств теперь 60
(было 65, за счёт стажа +10 макс). Формула прироста за партнёрства
(`CONFIRMATION_WEIGHT`) не менялась.

## GURO ID — приватность профиля (тумблеры), 10.08.2026

По просьбе владельца: сам пользователь решает в Mini App, что из его анкеты
видно ЧУЖИМ в поиске. **Opt-in модель (переведено 10.08.2026 вечером,
исходно был opt-out):** по умолчанию поля СКРЫТЫ, владелец сам включает то,
что хочет показать. Партнёрства (с кем сотрудничал) видны ВСЕГДА — их
нельзя скрыть ни одним тумблером, это ядро смысла GURO ID (подтверждение
репутации через сотрудничество); так же всегда виден `username` (иначе
искать/идентифицировать человека нечем).

**Схема:** 5 колонок в `guro_users` (`show_name`, `show_company`,
`show_vertical`, `show_tenure`, `show_reputation`), добавлены мягкой
миграцией `_ensure_column` (тот же приём, что в `storage.py`), дефолт 0
(ничего не видно, opt-in). Список полей — `guro_constants.PRIVACY_FIELDS`.
**Изначально поля назывались `hide_*` с дефолтом "видно всем" (opt-out)** —
владелец попросил перевернуть логику; старые `hide_*` колонки остались в
БД неиспользуемым мусором (переименовывать/удалять не стали — реальных
данных там не накопилось, фича была невидна пользователю до этого же
вечера, см. ниже).

**API:**
- `GET /api/me` — теперь всегда отдаёт СВОИ данные полностью (тумблеры на
  собственный просмотр не действуют) + ключ `privacy` с текущим состоянием
  всех 5 тумблеров, чтобы фронт мог отрисовать переключатели. Отдаёт
  `privacy` ДАЖЕ для витрины разработчика (`is_showcase`) — это настройка
  аккаунта, не данные анкеты, иначе владелец (аккаунт `lizard_hoppers`)
  физически не увидел бы свои же тумблеры (витрина подменяет весь обычный
  экран профиля).
- `GET /api/search` — применяет `_apply_privacy()` к карточке ЦЕЛИ (не
  искателя) ПОСЛЕ пейволла по подписке: поля, которые НЕ включены,
  заменяются на `null` (ключ остаётся в JSON, просто `null` — фронту проще
  проверять `=== null`, чем отсутствие ключа). Действует одинаково что с
  подпиской, что без.
- `POST /api/privacy` — `{"field": "show_company", "value": true}`,
  валидирует `field` по `GC.PRIVACY_FIELDS` (400 `UNKNOWN_FIELD` на
  опечатку/чужой ключ), возвращает актуальное состояние всех тумблеров.

**Фронтенд:** переключатель (`.switch` в `styles.css`) — включено = золото,
ползунок вправо; выключено = серый, ползунок влево (стандартная конвенция,
владелец явно попросил именно так). Подпись под тумблером: "Видно всем в
поиске" (включён) / "Скрыто от чужого поиска" (выключен, дефолт).

**Тесты:** `guro_id: storage` (get_privacy/set_privacy_field, дефолт всё
False, unknown field) + блок приватности внутри `guro_id: API (aiohttp
routes)` (по умолчанию всё скрыто в чужом поиске кроме username/партнёрств,
включение тумблера показывает поле, партнёрства видны всегда, неизвестное
поле -> 400). PASS=3382 FAIL=0. Проверено ТАКЖЕ напрямую на живом проде
(не только в тестах): сгенерирован настоящий подписанный initData под
реальный `user_id` владельца, curl по `127.0.0.1:8092/api/me` подтвердил
`privacy` со всеми `false` в реальной БД.

**Блокер снят 10.08.2026: `webapp/` пересобран с нуля.** Прежние исходники
React-приложения нигде не сохранились (собирались когда-то в одноразовом
окружении, на сервер копировался только `dist/`) — реконструировал их заново
по декомпилированному `dist/` (дизайн-токены/CSS-классы/копирайт-тексты
вытащены из минифицированного бандла grep'ом, структура данных витрины
разработчика — из `guro_showcase.py`), плюс сразу вкрутил новый функционал
приватности (тумблеры). **На этот раз исходники ЗАКОММИЧЕНЫ в git**
(`webapp/*.jsx`, `package.json`, `vite.config.js`, `smoke.mjs`) — не должны
потеряться снова. `webapp/node_modules` в `.gitignore`, ставить заново
`npm install` перед следующей сборкой.

**Стек:** React 18 + Vite 5 + Framer Motion (как и раньше). 4 таба —
Профиль/Поиск/Подтвердить/Подписка, + витрина разработчика
(`DeveloperShowcase.jsx`, для `lizard_hoppers`), + анимированная слот-машина
на старте (`SlotIntro.jsx`, декоративная, тема казино-комьюнити), + новая
секция «Приватность» с 5 тумблерами прямо в Профиле (`PrivacyToggles.jsx`).
Скрытые приватностью поля рендерятся как «🔒 Скрыто» (`IdentityLine` в
`Shared.jsx`), отличая это от «просто не заполнено в анкете» (`null` vs
`undefined`/пусто — см. `guro_id_api._apply_privacy`, которая именно
обнуляет поле, а не убирает ключ).

**Собирается ЛОКАЛЬНО** (на сервере всё ещё нет Node): `cd webapp && npm
install && npm run build`, потом скопировать `dist/` на сервер — рестарт
`guro-id-api.service` НЕ нужен (aiohttp отдаёт статику с диска на каждый
запрос, проверено). **Смоук-тест** `webapp/smoke.mjs` — headless Chromium
(Playwright) с замоканными `/api/*` и `Telegram.WebApp`, проходит по всем
табам, ловит console-ошибки, сохраняет скриншоты каждого экрана; не входит
в прод-сборку, ставить отдельно (`npm install --no-save playwright && npx
playwright install chromium`). Прогнан перед деплоем 10.08.2026 — 0 ошибок
на всех 4 табах + витрине + переключении тумблера.

**Деплой 10.08.2026:** новый `dist/` (хэши файлов
`index-DOsfhMqR.js`/`index-CdW4G1jl.css`) уже на проде, проверено живым
curl-запросом на `https://guro-app.193.111.62.16.sslip.io/`.


## GURO ID — два тарифа подписки + оплата в крипте, 10.08.2026

По просьбе владельца: раньше была одна подписка (150⭐/30 дней). Теперь два
тарифа — месячный и годовой (со скидкой), плюс альтернативный способ оплаты
через крипту (CryptoBot), не только Telegram Stars.

**Тарифы** (`guro_constants.SUBSCRIPTION_PLANS`):
- `monthly` — 30 дней, **500⭐**.
- `yearly` — 365 дней, **5000⭐** (вместо "честных" 500×12=**6000⭐** —
  `stars_price_full` хранится отдельно специально для скидочной плашки
  во фронте, "экономия 1000⭐").

**Крипто-цены** считаются от Stars-цены через `STARS_TO_USD_RATE = 0.015`
(константа в `guro_constants.py`) — **это НЕ живой курс**, Telegram не
публикует единую ставку Stars→USD (только цену IAP-пакетов Apple/Google,
которая сама плавает ~$0.013–0.02/⭐ по размеру пакета). Раз в несколько
месяцев стоит вручную сверить с актуальными ценами Stars в App
Store/Google Play и поправить константу, если разъехалось.

**Схема данных:** `activate_subscription(user_id, duration_days)` теперь
принимает длительность параметром (раньше — захардкожено 30 дней). Payload
Stars-инвойса и crypto-инвойса — единый формат
`guro_id_subscription:<plan>:<user_id>` (было просто `guro_id_subscription:
<user_id>`) — план читается на этапе подтверждения оплаты
(`handlers/guro_payments.py::_plan_from_payload`, `guro_id_api.
handle_crypto_webhook`), неизвестный план — тихий откат на `monthly` (не
должно случаться в проде, план валидируется ещё при создании инвойса).

**API:**
- `GET /api/plans` — публичный (без авторизации), единый источник цен для
  фронта: Stars-цена, "полная" цена (только у yearly), крипто-эквивалент,
  и `crypto_enabled` (true только если задан `CRYPTOBOT_API_TOKEN`) — фронт
  по этому флагу прячет кнопку крипто-оплаты вместо того, чтобы биться в 503.
- `POST /api/subscribe` — теперь принимает `{"plan": "monthly"|"yearly"}`
  в теле (400 `UNKNOWN_PLAN` без него/с опечаткой).
- `POST /api/subscribe/crypto` — то же самое, но создаёт инвойс CryptoBot
  вместо Telegram-инвойса, возвращает `pay_url` (открывается через
  `openTelegramLink`, не `openInvoice` — это НЕ платёж внутри Telegram, а
  переход в @CryptoBot). 503, если `CRYPTOBOT_API_TOKEN` не задан.
- `POST /api/crypto/webhook` — сюда CryptoBot шлёт POST при оплате.
  Подпись (`crypto-pay-api-signature`, HMAC-SHA256 тела запроса, ключ —
  SHA256(токена)) проверяется ОБЯЗАТЕЛЬНО — без этого кто угодно мог бы
  дёрнуть адрес и активировать себе подписку бесплатно.

**Модуль `guro_crypto.py`** — тонкая обёртка над Crypto Pay API
(`create_invoice`, `get_invoice` — резерв на случай проблем с вебхуком,
`verify_webhook_signature`). Никакой бизнес-логики, только HTTP.

**Статус на 10.08.2026: токен получен и настроен.** Приложение в CryptoBot
называется "Nipping Cat App" (app_id 620338). `CRYPTOBOT_API_TOKEN` вписан
в `.env`, вебхук указан в дашборде CryptoBot и подтверждён вживую (реальный
`getMe`, реальный инвойс через `create_invoice` — создан и сразу удалён,
реальный подписанный POST на `/api/crypto/webhook` на публичный HTTPS —
подписка активировалась и была вручную вычищена после проверки). Раздел
ниже (шаги 1-4) оставлен как справка "как это было настроено", не как
открытый TODO.
1. Открыть **@CryptoBot** в Telegram → **Crypto Pay** → **My Apps** →
   **Create App** → скопировать API-токен.
2. Вписать его в `.env` на сервере: `CRYPTOBOT_API_TOKEN=<токен>`,
   перезапустить `guro-id-api.service`.
3. В настройках приложения в дашборде CryptoBot включить **Webhooks** и
   указать URL: `https://guro-app.193.111.62.16.sslip.io/api/crypto/webhook`.
4. Проверить: `GET /api/plans` должен начать отдавать `"crypto_enabled":
   true`, кнопка крипто-оплаты появится в Mini App автоматически (условный
   рендер по этому флагу, фронт трогать не нужно).

**Фронтенд (`SubscribeScreen.jsx`):** две карточки тарифов (клик —
переключает выбор), у годового — зачёркнутая "полная" цена + плашка
"экономия N⭐". Кнопка Stars всегда видна, кнопка крипто — только если
`crypto_enabled=true`. Оплата в крипте — асинхронная (в отличие от Stars,
где `openInvoice` даёт колбэк со статусом сразу): открываем `pay_url`
через `openTelegramLink`, статус подписки обновится при следующем
открытии профиля (после того, как долетит вебхук).

**Тесты:** новый блок в `guro_id: API (aiohttp routes)` — оба тарифа в
`/api/plans`, скидочная цена, отказ без плана/с неизвестным планом,
`/api/subscribe/crypto` возвращает 503 без токена и 200 с моком CryptoBot,
вебхук отклоняет неверную подпись (подписка НЕ активируется) и принимает
верную (подписка активируется на правильный срок — проверено, что план
`yearly` даёт ~365 дней, а не дефолтные 30), чужой `update_type`
игнорируется без побочных эффектов. PASS=3418 FAIL=0. Проверено также
напрямую на живом проде: `GET /api/plans` отдаёт реальные 500⭐/5000⭐
(6000⭐ "было бы"), крипто-цены 7.5/75 USDT, `crypto_enabled: true` (токен
настроен, см. выше).

## GURO ID — редизайн экрана профиля (визитка + подразделы), 10.08.2026

По ручному эскизу заказчика (фото от владельца): вместо одного длинного
экрана профиля со всеми полями подряд — карточка-визитка наверху (аватар
из Telegram или буква-заглушка, имя, должность, компания, бейдж
вертикали) + меню из 4 пунктов, каждый открывает свой подэкран:
- **Мой рейтинг** — метрики (репутация/партнёрства/стаж) + список
  партнёрств (перенесено из старого единого экрана как есть).
- **Моё CV** — новое текстовое поле, редактируется прямо в Mini App.
- **Мои контакты** — Telegram (всегда виден, иначе не найти в поиске) +
  LinkedIn (только для чтения, из анкеты бота) + сайт (новое editable-поле).
- **Мои офферы** — "я ищу" (только для чтения, это поле `request`/
  "что актуально" из анкеты бота, семантически совпадающее с "ищу") +
  "я полезен" (новое editable-поле).

**Новые поля** (`guro_constants.EXTRA_PROFILE_FIELDS = ("cv_text",
"website", "offering")`) — TEXT-колонки в `guro_users`, редактируются
через новый `POST /api/profile` (`{"field": ..., "value": ...}`, 400
`UNKNOWN_FIELD` на неизвестное поле). Хранилище (`guro_storage.
get_extra_profile`/`set_extra_profile_field`) полностью generic —
работает от списка констант, ничего не хардкожено под конкретные поля.

**Приватность расширена до 9 полей** (было 5): добавлены `show_profession`,
`show_cv`, `show_contacts`, `show_offers`. Как и раньше — всё по умолчанию
`False` (скрыто), редактируется тумблерами прямо под тем подэкраном,
которому поле принадлежит (а не одним списком на весь профиль сразу).
`PrivacyToggles.jsx` теперь принимает проп `fields` — рендерит только
переданные тумблеры, у каждого подэкрана свой набор.

**Витрина разработчика (`DeveloperShowcase`)** — не тронута логически,
но теперь под ней рендерится ПОЛНЫЙ список из всех 9 тумблеров разом
(`Object.keys(PRIVACY_LABELS)`), а не 4 как раньше — иначе автор бота не
увидел бы тумблеры новых полей на своём же аккаунте.

**Стрелки на эскизе владельца** — по его уточнению, это только пояснения
"что где" для меня, не элемент дизайна; в интерфейсе стрелок нет.

**Новые компоненты** (`webapp/src/components/profile/`): `ProfileHub.jsx`
(визитка+меню), `RatingSubscreen.jsx`, `CvSubscreen.jsx`,
`ContactsSubscreen.jsx`, `OffersSubscreen.jsx` — у всех подэкранов общий
паттерн: кнопка "‹ Профиль" сверху, `PrivacyToggles` с нужным поднабором
`fields` снизу. `ProfileScreen.jsx` теперь просто роутер между витриной/
хабом/4 подэкранами по локальному `useState`.

Новый переиспользуемый `EditableField` (`Shared.jsx`) — режим просмотра
(значение или "не заполнено" + кнопка "Заполнить"/"Изменить") ↔ режим
редактирования (textarea/input + "Сохранить"/"Отмена"), используется всеми
тремя editable-полями (CV, сайт, "я полезен").

**Тесты:** `simulate_bot_tests.py` — новые поля по умолчанию скрыты, после
точечного включения тумблера видны; `POST /api/profile` (400 на неизвестное
поле, 200 + эхо значения на валидное). PASS=3418 FAIL=0. Плюс headless
Playwright-смоук (`webapp/smoke.mjs`) — заход в каждый из 4 подэкранов и
обратно, реальное редактирование CV-поля через UI со скриншотами; всё ok,
консоль чистая (кроме ожидаемого заблокированного запроса к
telegram-web-app.js).

## GURO ID — статус трудоустройства + QR-код профиля, 10.08.2026

По просьбе владельца (заказчика): (1) публичный статус трудоустройства
("🟢 ищу работу" / "❕ нейтральный" / "🔴 уже работаю" / "Выкл") в верхней
части профиля; (2) «Мой QR» — QR-код, открывающий профиль владельца, но
карточку целиком видно только подписчикам (тот же пейволл, что у поиска).

**Статус трудоустройства** — сознательное исключение из пейволла и из
opt-in модели приватности: виден ВСЕМ бесплатно, даже без подписки
смотрящего (как "Open to Work" в LinkedIn — весь смысл в том, что его
видят другие). НЕ входит в `GC.PRIVACY_FIELDS`/`_PRIVACY_FIELD_MAP` —
отдельная колонка `work_status` (TEXT, мягкая миграция), 4-е состояние
"выкл" = NULL. Валидация закрытого набора значений (`GC.WORK_STATUS_
VALUES = ("looking","neutral","working")`) — новый эндпойнт
`POST /api/work_status` (НЕ через generic `set_extra_profile_field`, т.к.
это не свободный текст), 400 `INVALID_STATUS` на прочее. Появляется и в
`_profile_summary`, и — намеренно — в урезанной locked-карточке
`handle_search` (наравне с username/reputation_score-тизером).
Фронт: `WorkStatusPicker`/`WorkStatusBadge` (`Shared.jsx`) — сегментированный
переключатель в `ProfileHub.jsx` под визиткой + бейдж в `ResultCard`
(`SearchScreen.jsx`), рендерится и в locked, и в полной карточке.

**QR-код профиля.** Технический выбор (обсуждён с владельцем): QR кодирует
классический deep-link `t.me/<bot>?start=guro_<user_id>`, А НЕ прямую
ссылку на Mini App — та требует отдельной регистрации short name через
@BotFather (`startapp`-механизм), которой у бота нет, и чтобы не заводить
такую ручную настройку, выбран более простой путь через обычный `/start`.
Сканирующий получает от бота сообщение с web_app-кнопкой на
`<guro_id_webapp_url>/?target=<id>`.

**Важный нюанс в `handlers/flow.py::start()`:** голый `/start` у бота
ВСЕГДА возвращает к `lang_select` (даже у уже зарегистрированных — см.
раздел выше про смарт-подсказку верификации/повторный вход), поэтому для
QR понадобилась ОТДЕЛЬНАЯ ветка ДО этой логики: `?start=guro_<id>` у
requester'а с `has_profile()==True` -> сразу шлём web_app-кнопку и `END`,
БЕЗ повторного прохождения анкеты. Если сканирующий НЕ зарегистрирован —
падает в обычный флоу регистрации, просто теряя цель QR (мелкий, приемлемый
edge-case: без анкеты GURO ID всё равно недоступен).

**Фронт (`App.jsx`):** `?target=<id>` читается из `window.location.search`
ОДИН раз при старте (до первого рендера, решает стартовый таб), URL сразу
чистится через `history.replaceState` (чтобы обновление страницы не
повторяло deep-link). `SearchScreen.jsx` получает `deepLinkTargetId`,
дёргает НОВЫЙ `GET /api/search?user_id=<id>` (в дополнение к обычному
`?username=`, ID стабильнее, юзернейм мог смениться), рендерится тем же
кодом, что обычный поиск — тот же `LockedOverlay`/пейволл. Новый
`ProfileHub` пункт меню «Мой QR» -> `QrSubscreen.jsx`, QR рисуется
КЛИЕНТСКИ (пакет `qrcode`, `QRCode.toDataURL()`) из ссылки, отданной новым
`GET /api/qr` (эндпойнт собирает её на бэкенде из `settings.bot_username` —
новое поле конфига, дефолт `GamblingCommunitybot`, переопределяется
`BOT_USERNAME` в `.env`).

**Тесты:** `simulate_bot_tests.py` — `/api/work_status` (валидация,
видимость в locked-карточке НЕзависимо от подписки, "выкл" прячет), `/api/qr`
(дипссылка, 401 без авторизации), бот-сторона `/start guro_<id>` (END без
повторной анкеты у зарегистрированных, обычный флоу у незарегистрированных,
кнопка — именно `web_app`, не url-ссылка). PASS=3435 FAIL=0. Плюс
headless-смоук: клик по статусу, заход в «Мой QR» (реальный QR-image в DOM),
и ОТДЕЛЬНЫЙ сценарий — открытие страницы сразу с `?target=555` в URL,
подтверждено автопопадание на таб «Поиск» с блокированной карточкой цели
(бейдж статуса виден СКВОЗЬ блюр пейволла) и очистка URL после загрузки.
Проверено также напрямую на живом проде реальным подписанным initData:
`GET /api/qr` вернул корректную ссылку на реальный `user_id`,
`POST /api/work_status` — установка/сброс статуса с реальной валидацией
(400 на мусорное значение), тестовое значение убрано после проверки.
**НЕ проверено вживую** — реальный скан QR камерой телефона через настоящий
Telegram-клиент (только автотест логики `flow.py` напрямую, не через живой
polling бота) — стоит владельцу попробовать самому при случае.

## GURO ID — новая цена подписки + «рейтинг сгорает без подписки», 10.08.2026

Заказчик поднял цену подписки до долларового ориентира ($10/мес, $99/год)
и добавил новый экран первого запуска (онбординг) с полным питчем — по
ручному эскизу. Уточнил у владельца 2 момента через AskUserQuestion перед
тем, как трогать формулу репутации и реальные пользовательские данные:
(1) технически «рейтинг сгорает» = скрывается, НЕ удаляется физически —
безопаснее, не рискуем стереть чужие заработанные баллы навсегда из-за
бага/спорной оплаты; (2) звёздная цена — 650⭐/мес и 6600⭐/год (округление
до красивых чисел от $10/$99 при `STARS_TO_USD_RATE=0.015`, годовой
получился РОВНЫМ числом без округления).

**Новая цена** (`guro_constants.SUBSCRIPTION_PLANS`): `monthly` 650⭐ (было
500⭐), `yearly` 6600⭐ (было 5000⭐), `stars_price_full=7800` (650×12, для
зачёркнутой скидочной плашки — было 6000).

**«Рейтинг сгорает без подписки» — реализовано как РЕВЕРСИВНЫЙ гейт, не
удаление.** Новая независимая ось видимости (отдельно от opt-in privacy-
тумблеров и от пейволла поиска): пока у ВЛАДЕЛЬЦА профиля нет активной
подписки — `reputation_score`/`confirmed_partnerships`/`partners` скрыты
ото ВСЕХ, включая его самого, даже в `/api/me`. Значения в БД НЕ трогаются
— как только подписка возобновляется, старое число мгновенно возвращается
(проверено тестом: реактивация возвращает ТО ЖЕ значение, не сброс на
базу 50). Новая функция `guro_id_api._apply_subscription_gate()`,
применяется в `handle_me` (после `_profile_summary`) и в `handle_search`
(и в locked-тизере, и в полной карточке — если у ЦЕЛИ поиска нет
подписки, репутацию не увидит НИКТО, независимо от того, подписан ли сам
ищущий). `work_status` в гейт НЕ входит — остаётся бесплатным маркером
всегда, по прежнему решению.

Фронт: `RatingSubscreen.jsx` — новый баннер "Рейтинг и сделки скрыты" с
объяснением и кнопкой "Оформить подписку" (переход на таб Подписка),
показывается когда `!profile.is_subscribed`; метрики при этом всё равно
рендерятся через существующий `MetricsRow` (значения `null` -> 🔒, уже
была такая логика). `SubscribeScreen.jsx` — переписан текст сравнения
тарифов (было "рейтинги видно всем бесплатно", стало честно: рейтинг
теперь ПЛАТНЫЙ актив, бесплатен только статус трудоустройства).

**Экран первого запуска (онбординг)** — по ручному эскизу владельца:
раньше при отсутствии анкеты (`NO_PROFILE`) показывался один абзац
"Профиль не найден". Теперь `ProfileScreen.jsx` рендерит новый
`OnboardingScreen.jsx`: макет ID-карты с плейсхолдерами (Name?/Position?/
Company?, полупрозрачные, курсивом — явно "пример", не реальные данные),
короткое объяснение, раскрывающаяся по клику на "Подробнее" (без внешних
стрелок — по уточнению владельца, на эскизе они были только пояснением
для меня) секция с полным питчем: список 7 вертикалей (Гемблинг/Бейтинг/
Крипто/Нутра/Дейтинг/Е-коммерс/Другое), механика подтверждения сделок,
CV/поиск работы, объяснение гейта подписки (формулировка НАМЕРЕННО чуть
смягчена под реальное поведение — "рейтинг скрывается" вместо буквального
"обнуляется" у заказчика, чтобы не вводить пользователей в заблуждение
относительно необратимости; если владелец настаивает на буквальной
формулировке — чисто текстовая правка, `OnboardingScreen.jsx`), новая
цена. CTA-кнопка "Заполнить анкету в боте" (`openTelegramLink` на
`t.me/GamblingCommunitybot`).

**Тесты:** `simulate_bot_tests.py` +9 проверок — гейт скрывает/показывает
себе и в чужом поиске (в т.ч. НЕ трогает privacy-поля вроде company),
реактивация возвращает СТАРОЕ значение (не 50), обновлены 4 устаревшие
проверки цены (500/5000/6000 -> 650/6600/7800). PASS=3444 FAIL=0.
Headless-смоук: гейт-баннер виден без подписки/пропадает с подпиской (те
же числа возвращаются — скриншотами подтверждена реверсивность),
онбординг-экран рендерится на `NO_PROFILE` + разворачивание "Подробнее"
показывает весь питч. Проверено и на живом проде: `GET /api/plans`
отдаёт новые 650⭐/6600⭐/7800⭐.

## ОТКРЫТО / TODO (GURO ID)
- [ ] Подтвердить у владельца `CONFIRMATION_WEIGHT` (цена подписки уже
      зафиксирована 10.08.2026 вечером — 650⭐/6600⭐).
- [ ] Сквозной прогон эталонного сценария ТЗ п.2 на реальных аккаунтах.
- [ ] Живой скан QR-кода профиля камерой телефона (см. раздел выше) — код
      проверен автотестами и напрямую curl'ом, но не самим сканированием.
- [ ] Владельцу подтвердить смягчённую формулировку в онбординге
      ("рейтинг скрывается" вместо буквального "обнуляется") — если
      настаивает на жёсткой версии, это чисто текстовая правка.
- [ ] Решить: остаться на sslip.io или переехать на купленный поддомен GURO.
- [ ] Т.к. это первая реализация Mini App в проекте — при добавлении
      следующих модулей (заявка на нетворкинг, реф-центр, колесо фортуны из
      плана 04.08) переиспользовать `webapp/` каркас (табы) и `guro_id_api.py`
      как основу общего API, а не плодить отдельные сервисы/домены.
- [ ] Пересобранный `webapp/` — реконструкция по CSS/текстам из старого
      бандла, не byte-perfect копия исходного (которого больше нет).
      Стоит один раз пройтись глазами в реальном Telegram-клиенте
      (не только в headless-смоуке) и свериться с тем, что видел владелец
      раньше — вдруг где-то разошлись мелкие детали вёрстки/анимаций.
